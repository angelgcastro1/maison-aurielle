/* Self-hosted mode: every image and video is served from this project's
   own /assets folder. If a local file is ever missing, media.js falls back
   to the original CDN copy automatically. */
window.AURIELLE_USE_LOCAL = true;

/* Default: stream media from the CDN. localize.sh overwrites this
   file with `window.AURIELLE_USE_LOCAL = true;` after downloading
   all assets, switching the site to fully-local, offline media. */
window.AURIELLE_USE_LOCAL = false;
